# .env Datei für Nginx Proxy Manager
# Kopiere diese Datei zu .env und passe die Werte an

# === DOMAIN KONFIGURATION ===
EXTERNAL_DOMAIN=myhomeassi23.ddns.net
INTERNAL_DOMAIN=chef.fritz.box
HOSTNAME=chef

# === NETZWERK KONFIGURATION ===
# IP-Bereich für Docker Netzwerk
DOCKER_SUBNET=172.20.0.0/16

# === PORTS ===
# Hauptports (normalerweise nicht ändern)
HTTP_PORT=80
HTTPS_PORT=443
ADMIN_PORT=81

# === SICHERHEIT ===
# Basic Auth Realm Name
AUTH_REALM="Nginx Proxy Manager Admin"

# === SSL KONFIGURATION ===
# Pfade für SSL Zertifikate (relativ zum Projektverzeichnis)
SSL_CERT_PATH=./nginx-config/ssl
LETSENCRYPT_PATH=./config/letsencrypt

# === EMAIL FÜR LET'S ENCRYPT ===
# Deine Email-Adresse für Let's Encrypt Benachrichtigungen
LETSENCRYPT_EMAIL=admin@example.com

# === BACKUP KONFIGURATION ===
# Pfad für automatische Backups
BACKUP_PATH=./backups

# === LOGGING ===
# Log Level (error, warn, info, debug)
LOG_LEVEL=info

# === ZEITZONE ===
# Zeitzone für Container
TZ=Europe/Berlin
